import GoogleBtn from "../googleButton/GoogleBtn";

const Menu = () => {
  return (
    <section
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        minHeight: "100vh",
        color: "black",
        backgroundColor: "#eaf681"
      }}
    >
      Sección Menú
    </section>
  );
};

export default Menu;